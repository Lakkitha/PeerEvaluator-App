pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'Frontend'

// Comment out the problematic line
// apply from: file("../node_modules/@react-native/gradle-plugin/settings.gradle")

// Just include the app project
include ':app'

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven { url "https://www.jitpack.io" }
        maven { url '../node_modules/react-native/android' }
    }
}

println "Using simplified settings without external dependencies"
